import { assertEquals } from "@std/assert";
import { solvePart2 } from "./part2.ts";

Deno.test(function part2_example() {
  const exampleInput = ``;
  assertEquals(solvePart2(exampleInput), -1);
});
